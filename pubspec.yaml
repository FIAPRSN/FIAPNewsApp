name: fiap_newsapp
description: Aplicativo realizado como trabalho final da matéria de Flutter
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: '1.0.0+1'

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  firebase_auth: ^4.2.9
  firebase_core: ^2.7.0
  flutter:
    sdk: flutter
  get: ^4.6.5
  google_fonts: ^4.0.3
  http: ^0.13.5
  url_launcher: ^6.1.10
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/